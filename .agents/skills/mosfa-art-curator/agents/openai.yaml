interface:
  display_name: "MoSFA Art Curator"
  short_description: "Run artwork acquisition workflows"
  default_prompt: "Use $mosfa-art-curator to run full acquisition mode for a new MoSFA artwork: use subagents/delegation where useful, generate and critique candidates, make a contact sheet, and ask me to approve before accessioning."

policy:
  allow_implicit_invocation: true
